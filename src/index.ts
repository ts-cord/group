import { version as _version } from '../package.json';

/**
 * The [@ts-cord/group](https://npmjs.com/package/@ts-cord/group) version that you are currently using.
 */
export const version: string = _version;
export { Group } from './group';